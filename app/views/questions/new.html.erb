<h1>質問の新規作成</h1>
<h2>気になることや不安なこと、なんでも聞いてみよう</h2>
<div class="container question-input">
  <%= form_with model: @question, local: true, data: { turbo: false } do |f| %>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class:"form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class:"form-control"  %>
    </div>
  
    <div class="form-group">
      <%= f.label :content %>
      <%= f.text_area :content, class:"form-control", size: "20x10"  %>
    </div>

    <%= f.submit "投稿", class: "btn" %>
  <% end %>
</div>
